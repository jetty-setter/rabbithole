[pytest]
# Make each component importable by its own top-level name:
#   app.main (api/) · worker (worker/) · handler (lambdas/transcribe/)
pythonpath = api worker lambdas/transcribe
testpaths = api/tests worker/tests lambdas/transcribe scripts/tests
python_files = test_*.py
